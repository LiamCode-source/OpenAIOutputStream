# OpenAIOutputStream - Version 1.0
Mendix module used for streaming OpenAI outputs.

Using the REST API action for OpenAI in Mendix is the perfect solution... until you need to stream the output.

This module provides a JS action which can be used to stream an OpenAI output as the chunks are recieved, allowing the output to be streamed to the end user as the output is generated
